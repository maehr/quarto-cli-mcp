@AGENTS.md

## Claude Code

Use `AGENTS.md` as the canonical machine contract. The lines below add what is specific to Claude Code.

- `pnpm check` after a normal change batch
- `pnpm check` and the end-to-end steps in `SPECS.md` before a handoff or a release
- Use plan mode for a change to `SPECS.md`
